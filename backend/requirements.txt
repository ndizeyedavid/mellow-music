# Web framework (fastapi[standard] provides the `fastapi` CLI the platform boots with)
fastapi[standard]

# Database
mysql-connector-python==26.7.0

# Music APIs (yt-dlp floats forward for YouTube bot-fix updates)
yt-dlp>=2026.8.19
spotipy==2.26.0

# Custom packages
customisedLogs==2.0.3
rateLimitedQueues==0.0.3.post1
randomisedString==1.4.1
autoReRun==1.3.0

# Security / crypto
cryptography==50.0.1

# HTTP
requests==2.34.2

# Cache
redis==8.1.0

# Transitive pin: colr<=0.9.0 breaks on Python 3.13 (typing.io removed).
# customisedLogs leaves it unpinned, so pin the fixed release explicitly.
colr==0.9.1
